#pragma once

// Copy this file to secrets.h and update the values for your local network.
// secrets.h is ignored by Git so private Wi-Fi details are never uploaded.
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
#define UNITY_PC_IP IPAddress(192, 168, 1, 100)
